using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// <summary>
/// This script literally exists to let Tank Test know that Hi, i'm a turret and use me specifically for the .gameobject to reference
/// a transform for the network tank script.
/// </summary>
public class Turret : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
