using UnityEngine;

public class PowerupSpawner : MonoBehaviour
{
    // create a spawner that randomly spawns a random powerup
    // another powerup can only spawn once the previous one has dissapeared or been picked up
    //
}
